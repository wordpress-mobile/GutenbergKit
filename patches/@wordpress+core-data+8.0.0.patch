diff --git a/node_modules/@wordpress/core-data/build-module/resolvers.mjs b/node_modules/@wordpress/core-data/build-module/resolvers.mjs
index d892e2d..233c398 100644
--- a/node_modules/@wordpress/core-data/build-module/resolvers.mjs
+++ b/node_modules/@wordpress/core-data/build-module/resolvers.mjs
@@ -468,9 +468,15 @@ var canUser = (requestedAction, resource, id) => async ({ dispatch, registry, re
   } catch {
     return;
   }
-  const permissions = getUserPermissionsFromAllowHeader(
-    response.headers?.get("allow")
-  );
+  const allowedMethods = response.headers?.get("allow");
+  // `Allow` is only readable same-origin, or when the site lists it in
+  // `Access-Control-Expose-Headers`, which WordPress does not. Its absence
+  // says nothing about the user's capabilities, so leave `canUser`
+  // `undefined` rather than reporting every action as denied.
+  if (!allowedMethods) {
+    return;
+  }
+  const permissions = getUserPermissionsFromAllowHeader(allowedMethods);
   const receiveUserPermissionArgs = {};
   const canUserResolutionsArgs = [];
   for (const action of ALLOWED_RESOURCE_ACTIONS) {
