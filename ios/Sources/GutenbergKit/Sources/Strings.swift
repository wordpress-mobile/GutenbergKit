import Foundation

enum Strings {
    static let loadingEditor = NSLocalizedString(
        "org.wordpress.gutenbergkit.loading-editor",
        value: "Loading Editor",
        comment: "A message displayed to the user under a progress bar while the editor is loading"
    )
}
