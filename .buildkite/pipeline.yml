agents:
  queue: mac

env:
  IMAGE_ID: $IMAGE_ID

steps:
  - label: ":swift: Build Swift Package"
    command: swift build
    plugins: &plugins
      - $CI_TOOLKIT_PLUGIN
      - $NVM_PLUGIN

  - label: ":react: Build React App"
    command: make build
    plugins: *plugins
